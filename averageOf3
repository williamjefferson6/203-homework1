#include <iostream>
using namespace std;
float averageOf3(int x, int y, int z)
{
	float avg = 0;
	avg = (x + y + z) / 3.0;
	return avg;
}
int main()
{
	int x, y, z;
	cin >> x;
	cin >> y;
	cin >> z;
	cout << averageOf3(x, y, z) << endl;
}
